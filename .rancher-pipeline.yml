stages:
- name: golibe-compile
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: golive/app:${CICD_EXECUTION_SEQUENCE}
      pushRemote: true
      registry: harbor.bigbang.simbalink.cn
timeout: 60
notification: {}
